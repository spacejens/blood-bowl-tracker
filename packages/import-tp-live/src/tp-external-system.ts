/**
 * The external system TP records are registered under: tools/import-tp's own
 * default for its `externalSystemName` config key. The live import uses it
 * directly. The bulk import passes whatever name its config resolved.
 */
export const TP_EXTERNAL_SYSTEM_NAME = 'TP';
